function divideNumber(input) {
    var quotient = Math.floor(input / 7);
    var remainder = input % 7;
    console.log("Quotient: " + quotient);
    console.log("Remainder: " + remainder);
  }
  
  // Example usage
  var input = 25;
  divideNumber(input);
  